<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FrameDemos"
             x:Class="FrameDemos.MainPage"
             Title="Frame demos">
    <TableView Intent="Menu">
        <TableRoot>
            <TableSection>
                <TextCell Text="XAML Frame"
                          Detail="Create a frame with XAML."
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:FrameXamlPage}" />
                <TextCell Text="Code Frame"
                          Detail="Create a frame with code."
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:FrameCodePage}" />

            </TableSection>
        </TableRoot>
    </TableView>	
</ContentPage>
